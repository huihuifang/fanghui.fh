// 通过 NPM dependencies 成功安装 NPM 包后此处可引入使用
// 如安装 linq 包后就可以引入并使用这个包
// const linq = require("linq");

/**
 * @param {Params}  params     自定义参数
 * @param {Context} context    上下文参数，可通过此参数下钻获取上下文变量信息等
 * @param {Logger}  logger     日志记录器
 *
 * @return 函数的返回数据
 */
module.exports = async function (params, context, logger) {
  // 日志功能
 logger.info(`${new Date()} 函数开始执行1`);
 logger.info(`${new Date()} 函数开始执行2`);
 logger.info(`${new Date()} 函数开始执行3`);
 logger.info(`${new Date()} 函数开始执行4`);
 logger.info(`${new Date()} 函数开始执行5`);
 logger.info(`${new Date()} 函数开始执行6`);
 logger.info(` 函数开始执行7`);
 logger.info(` 函数开始执行8`);
 logger.info(` 函数开始执行9`);
 logger.info(` 函数开始执行10`);
 logger.info(`函数开始执行11`);
 logger.info(` 函数开始执行12`);
 logger.info(`函数开始执行13`);
 logger.info(`函数开始执行14`);
 logger.info(` 函数开始执行15`);
 logger.info(`函数开始执行16`);
 logger.info(` 函数开始执行17`);
 logger.info(` 函数开始执行18`);
 logger.info(` 657函数开始执行19`);
 logger.info(` 函数开始执行20`);
 logger.info(`函数开始执行21`);
 logger.info(`函数开始执行22`);
 logger.info(`函数开始执行23`);
 logger.info(`函数开始执行24`);
 logger.info(` 函数开始执行25`);
 logger.info(`函数开始执行26`);
 logger.info(` 函数开始执行27`);
 logger.info(` 函数开始执行28`);
 logger.info(` 函数开始执行29`);
 logger.info(` 函数开始执行30`);
  // 在这里补充业务代码
}